# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env
.env
.env.local
.env.*.local

# macOS
.DS_Store

# editor
.vscode/
.idea/
*.swp
*.swo

# opencode session state
.sisyphus/

# off-site brand assets (LinkedIn, Workspace, signatures), local-only
brand/

# workshop source material (PDFs, decks), local-only, root-level only
/workshops/

# scratch space, local-only: source material, drafts and working notes
/tmp/

# local-only leak-scan wordlist (the terms themselves are confidential)
.leakwords
